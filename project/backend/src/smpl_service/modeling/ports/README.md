# ports 目录说明

本目录定义后端可替换边界。端口只描述接口，不关心具体实现。

## 当前文件

| 文件 | 作用 |
| --- | --- |
| `input_port.py` | 输入适配器协议 |
| `model_port.py` | 模型后端协议和 `MeshData` |
| `output_port.py` | 输出写入协议 |

## 规则

- 新能力先确认是否需要端口。
- 端口不要依赖具体 adapter。
- 协议变更必须同步测试和 LLD。
