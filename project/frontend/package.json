{
  "scripts": {
    "prepare:samples": "npm run prepare:thuman && npm run prepare:smplx && npm run prepare:mocap",
    "prepare:thuman": "node scripts/prepare-thuman-sample.mjs",
    "prepare:smplx": "node scripts/prepare-smplx-sample.mjs",
    "prepare:mocap": "node scripts/prepare-mocap-samples.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node scripts/verify-samples.mjs"
  },
  "dependencies": {
    "three": "^0.164.0",
    "vite": "^5.0.0",
    "vue": "^3.5.35"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7"
  }
}
